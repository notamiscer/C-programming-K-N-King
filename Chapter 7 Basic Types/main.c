#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	float one = .10, two = .20, answer;
	
	answer = one + two;
	
	printf("%f + %f = %f", one, two, answer);
	return 0;
}
